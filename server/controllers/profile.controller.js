"use strict";

const ProfileController = (server) => {
  const {
    db: { Profile, User },
    boom,
    helpers: { paginator, filters },
  } = server.app;
  return {};
};

module.exports = ProfileController;
