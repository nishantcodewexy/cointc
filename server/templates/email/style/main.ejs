<style>
  @import url("https://fonts.googleapis.com/css2?family=Roboto:wght@300;900&display=swap");
  :root {
    --default-color: #333147;
    --primary-color: #0b5cbe;

    /* Size */
    --sm: 14px;
  }

  html,
  body {
    min-height: 100vh;
    font-family: "Roboto", "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    color: var(--default-color);
    font-size: var(--sm);
    background-color: #efefef;
  }

  body {
    max-width: 768px;
    margin: 0 auto;
    letter-spacing: 0.034em;
    line-height: 33px;
    font-size: 16px;
  }

  .heroHeader {
    display: flex;
    flex-direction: column;
    gap: 10px;
    padding: 16px;
    align-items: center;
    justify-content: center;
    min-height: 250px;
    background-color: #f6f6f6;
    margin-bottom: 16px;
  }
  a {
    color: var(--primary-color);
  }
  .extras {
    padding: 14px;
    background-color: white;
    border-radius: 8px;
    margin: 12px auto;
    text-align: center;
    font-weight: bolder;
    font-size: 2rem;
  }

  .cta {
    display: block;
    padding: 12px;
    background-color: var(--primary-color);
    width: 100%;
    margin: 8px auto;
    color: white;
    font-weight: 600;
    border-radius: 10px;
  }

  .conspicuous {
    color: var(--primary-color);
  }

  footer {
    padding: 12px 0;
  }
</style>
