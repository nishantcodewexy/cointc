{
  "name": "cointc",
  "version": "1.0.0",
  "main": "index.js",
  "author": "lil-armstrong <oebong1@gmail.com>",
  "license": "MIT",
  "scripts": {
    "server-dev": "yarn --cwd ./server dev",
    "server-build": "yarn --cwd ./server build",
    "server-test": "yarn --cwd ./server test",
    "client-dev": "yarn --cwd ./client dev",
    "client-build": "yarn --cwd ./client build && serve -s ./client/build",
    "client-test": "yarn --cwd ./client dev test"
  },
  "dependencies": {
    "@atlaskit/css-reset": "^6.1.4"
  }
}
